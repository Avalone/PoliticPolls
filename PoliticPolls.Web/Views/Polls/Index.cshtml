@model IEnumerable<PoliticPolls.DataModel.Poll>

@{
    ViewBag.Title = "Опросы";
}

<h2>Опросы</h2>

<p>
    @Html.ActionLink("Создать", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PollDate)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Respondent.Name)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PollDate)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.respondents.name)*@
        </td>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Подробнее", "Details", new { id=item.Id }) |
            @Html.ActionLink("Удалить", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
